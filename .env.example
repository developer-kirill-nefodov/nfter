# Root .env — consumed by the Makefile and docker/docker-compose.yml.
# The API and the site have their own .env files (see app/*/.env.example).

PROJECT_NAME=nfter

POSTGRES_DB=nfter
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
